"""
Job Service - Database access layer for jobs/calendar events.

This service handles all database operations for:
- Jobs (calendar events/appointments)
- Job history/audit trail

Only this service should directly access job-related tables.
"""
